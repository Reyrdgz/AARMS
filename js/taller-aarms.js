/**
 * Taller opcional (misma carpeta que la app). Sin dependencias externas.
 * Edita aquí pruebas; fusiona a documents-suite.js cuando esté listo.
 */
(function(){
'use strict';
window.TALLER_AARMS = window.TALLER_AARMS || { version: 1, ping(){ console.info('[Taller AARMS] ok'); return true; } };
})();
